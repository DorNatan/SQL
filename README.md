# SQL Data Analysis | Learn SQL by Doing #

## Running Tests

This project uses `pytest` for simple checks on the SQL files. After installing
`pytest`, run the following command from the repository root:

```bash
pytest
```
